/*

#pragma once

#include "journey.hpp"

struct journeynode {
    journey* dyrid;
    journeynode* next;

    journeynode(journey* dyrid) {
        this->dyrid = dyrid;
        this->next = nullptr;
    }

    ~DyraNode() {
        delete this->dyrid;
    }
    // Destructor ???
};

*/